// import { fromJS } from 'immutable';
// import { makeSelectDataDomain } from '../selectors';

// const selector = makeSelectDataDomain();

describe('makeSelectDataDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
