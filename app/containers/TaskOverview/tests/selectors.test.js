// import { fromJS } from 'immutable';
// import { makeSelectTaskOverviewDomain } from '../selectors';

// const selector = makeSelectTaskOverviewDomain();

describe('makeSelectTaskOverviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
